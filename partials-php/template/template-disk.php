<!-- TEMPLATE -->
<script id="disk-template" type="text/x-handlebars-template">
  <div class="card">
    <img src="{{{ poster }}}" alt="Poster">
    <h3>{{ title }}</h3>
    <span>{{ author }}</span>
    <span>{{ year }}</span>
  </div>
</script>
